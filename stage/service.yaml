apiVersion: v1
kind: Service
metadata:
  name: harness-lab-stage
  labels:
    app: harness-lab
    environment: stage
spec:
  type: LoadBalancer
  selector:
    app: harness-lab
    environment: stage
  ports:
    - name: http
      port: 80
      targetPort: 8080
