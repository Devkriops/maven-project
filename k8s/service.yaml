apiVersion: v1
kind: Service
metadata:
  name: harness-lab
  labels:
    app: harness-lab
spec:
  type: ClusterIP
  selector:
    app: harness-lab
  ports:
    - name: http
      port: 80
      targetPort: 8080
