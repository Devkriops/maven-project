apiVersion: v1
kind: Service
metadata:
  name: harness-lab
  namespace: <+infra.namespace>
  labels:
    app: harness-lab
spec:
  type: LoadBalancer
  selector:
    app: harness-lab
  ports:
    - name: http
      port: 80
      targetPort: 8080
