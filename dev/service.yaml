# apiVersion: v1
# kind: Service
# metadata:
#   name: harness-lab-dev
#   labels:
#     app: harness-lab
#     environment: dev
# spec:
#   type: LoadBalancer
#   selector:
#     app: harness-lab
#     environment: dev
#   ports:
#     - name: http
#       port: 80
#       targetPort: 8080
apiVersion: v1
kind: Service
metadata:
  name: harness-lab-dev
  labels:
    app: harness-lab
    environment: dev
spec:
  type: ClusterIP
  selector:
    app: harness-lab          # matches the pods (namespace isolates dev from stage)
  ports:
    - name: http
      port: 80
      targetPort: 8080
